# 개요

Figma `ui-ux` 파일의 **S05 — Cart**(장바구니) 화면을 구현했습니다.
뒤로가기 앱바, 옵션 요약과 라인별 수량 스테퍼가 있는 장바구니 라인 목록,
"메뉴 더 담기" 버튼, 주문금액·부가세·총 결제금액 breakdown, 그리고 하단 고정
주문 바로 구성됩니다.

이 프레임은 **주문하기 버튼의 금액이 부가세를 포함한 총액**임을 드러냅니다
(주문금액 23,000 + 부가세 2,300 = 25,300). S02 하단바도 같은 25,300원을
그리므로, 두 화면이 같은 계산을 공유하도록 정리했습니다. 자세한 내용은 아래
"교차 화면 정합" 항목에 있습니다.

## 변경 사항

### 화면

- `src/pages/CartPage.tsx` / `.css` — S05 화면 구성.

### 추가한 컴포넌트

| 컴포넌트 | Figma 노드 | 비고 |
|---|---|---|
| `CartLine` | `ext/CartLine` (15:100) | 56×56 썸네일 + 이름·옵션 요약·라인 합계·수량 스테퍼 |
| `PriceBreakdown` | `ext/PriceBreakdown` (15:129) | 상단 구분선 + 라벨/값 행, 총액 행은 22/700 |

### 수정한 컴포넌트

- `Button` — `large` 사이즈의 폰트를 `--type-body-strong`(16/700/24)에서
  신규 `--type-label-button-m`(15/700/22)으로 교정했습니다. 이 프레임의
  Large/Weak 버튼이 `Label/Button-M` 텍스트 스타일을 쓴다는 사실이 이번에
  처음 드러났습니다. S02·S04는 Large 버튼을 쓰지 않아 영향이 없습니다.
- `QuantitySelector` — `ariaLabel` prop 추가. S04는 "수량" 라벨 요소를
  `labelledBy`로 가리키지만, S05는 라인마다 스테퍼가 있어 가리킬 라벨 요소가
  없습니다. "김치찌개 수량"처럼 라인별 접근성 이름을 부여합니다.

### 지원 파일

- `src/utils/cart.ts` — 신규.
  - `calculateCartTotals(lines)` → `{ subtotal, vat, total }`, `VAT_RATE = 0.1`
  - `describeCartLineOptions(item, line)` → `"보통 · 공기밥 추가"`

### 추가한 디자인 토큰

- `--type-label-button-m: 700 15px / 22px` (Figma `Label/Button-M`)

### 교차 화면 정합

S05 프레임을 보기 전에는 S02 하단바의 25,300원이 라인 금액의 단순 합으로
보였습니다. 실제로는 부가세 포함 총액이었습니다. 이를 맞추기 위해:

- `MenuPage` 하단바가 `calculateCartTotals().total`을 쓰도록 변경했습니다.
- 목 장바구니를 S05 프레임과 동일하게 재구성했습니다.
  김치찌개(보통 · 공기밥 추가) 10,000 + 제육볶음(보통) 13,000 = 23,000,
  부가세 2,300, 총 25,300.
- 이에 맞춰 제육볶음의 옵션 그룹을 S04 때 임의로 만들었던 "양 선택"에서
  "맵기 선택"(필수, 기본값 보통)으로 교체했습니다. 프레임의 라인 요약 "보통"이
  실제 데이터에서 나오게 됩니다.

결과적으로 **하나의 목 데이터로 S02와 S05 프레임의 숫자가 동시에 일치**합니다.

기본값 없는 필수 그룹(담기 비활성화)의 사례는 비빔밥 "고추장 선택"에 그대로
남아 있어, S04의 검증 로직은 여전히 확인 가능합니다.

### 화면 연결

- `src/App.tsx` — `screen`에 `'cart'` 추가, 라인 수량 변경 핸들러 추가.
- `src/pages/MenuPage.tsx` — `onOpenCart` prop 추가. 앱바 장바구니 칩과 하단
  주문 바 모두 S05로 진입합니다 (UX-STRUCTURE §2).

여전히 `App.tsx`의 로컬 상태 기반이며 실제 라우팅의 임시 대체물입니다.

## Figma

기준 프레임: **`S05 — Cart`**, 노드 `15:95`
(`https://www.figma.com/design/u5pXNGrYEdVDbvqmJLglUS/ui-ux`).
MCP 응답의 현재 선택 노드와 프레임 이름이 일치함을 확인했습니다.

`AppBar`, `Button`, `QuantitySelector`, `BottomOrderBar`는 기존 컴포넌트를
재사용했습니다.

## 검증

- [x] `npm run lint` — 통과, 경고 없음
- [x] `npm run build` — `tsc -b && vite build` 성공
- [x] 브라우저 390×844에서 Figma와 비교

프레임 대비 실측(프레임 좌표 = 구현 좌표):

| 요소 | Figma | 구현 |
|---|---|---|
| AppBar | 0, 56 | 0, 56 |
| CartLine 1 | 72, 85 | 72, 85 |
| CartLine 2 | 173, 85 | 173, 85 |
| 썸네일 | x16, 56×56 | 16, 56×56 |
| PriceRow | x84, 125, w290 | 84, 125, w290 |
| QuantitySelector | x254, w120 | 254, w120 |
| 메뉴 더 담기 | 274, 48 | 274, 48 |
| PriceBreakdown | 338, 107 | 338, 108 |
| BottomOrderBar | 722, 122 | 721, 123 |

Large 버튼 실측: 15/700/22, r14, `rgb(232,243,255)` / `rgb(27,100,218)`.

동작 검증: 김치찌개 수량 2 → 라인 20,000원 · 주문금액 33,000 · 부가세 3,300 ·
총 36,300 · 하단바 36,300원으로 전부 일관되게 재계산. 수량 1에서 감소 버튼
비활성. "메뉴 더 담기"·뒤로가기 → S02, S02 장바구니 칩·주문 바 → S05.
S01 → S02 → S04 → S05 전 구간 이동 확인. 데이터 변경 후에도 S04 회귀 없음
(제육볶음 보통 기본 선택 13,000원, 비빔밥 필수 미선택 시 담기 비활성).
320px에서 가로 오버플로 없음.

## 참고 사항

**의도적인 차이**

- **PriceBreakdown / BottomOrderBar가 1px 큼.** Figma는 inside stroke라
  구분선이 높이에 포함되지만, CSS `border-top`은 1px을 더합니다. 요소의 시작
  좌표는 프레임과 일치하며 누적되지 않습니다. S04 옵션 행처럼 `outline`으로
  치환하는 방법은 한 변만 그릴 수 없어 적용하지 않았습니다.
- **부가세율 10%를 상수로 고정.** 프레임의 "부가세 (10%)" 표기를 따랐습니다.
  실제로는 매장 설정에서 내려와야 하므로 `VAT_RATE`로 한 곳에 모아 두었습니다.

**한계 / 후속 작업**

- **라인 삭제가 없습니다.** 수량 최소값은 1이고 감소 버튼이 비활성화됩니다
  (`ext/QtyStepper` 계약). 삭제는 D1 확인 다이얼로그와 함께 들어와야 합니다
  (UX-STRUCTURE §2.2). `ext/Dialog`는 아직 없습니다.
- **빈 장바구니 상태는 아직 도달 불가.** 삭제 수단이 없어서 코드 경로만
  존재합니다(방어적 fallback + 하단바 비활성). `ext/EmptyState`(§5.5)로
  교체되어야 합니다.
- 주문하기는 아직 no-op입니다. S06 주문 확인 화면이 다음 단계입니다.
- 최소 주문금액 미달(below-minimum), updating, line-unavailable 상태(§5.5)와
  주방 요청사항 `tds/TextField`(§3 S05 rank 6)는 프레임에 없어 범위 밖입니다.
- 장바구니는 여전히 메모리 상태이며 `localStorage` 영속화(§6.2)는 미구현입니다.
